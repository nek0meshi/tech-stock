query GetRecord($id: ID!) {
  record(id: $id) {
    id
    title
    url
    description
    imageUrl
    objectKey
    tags {
      id
      name
    }
    status
    rating
    memo
    readAt
  }
}
