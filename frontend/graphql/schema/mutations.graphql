type Mutation {
  createRecord(input: CreateRecordInput!): Record!
}

input CreateRecordInput {
  title: String!
  rating: Int!
  status: RecordStatus!
  memo: String!
  readAt: DateTime
  tags: [String!]!
}
